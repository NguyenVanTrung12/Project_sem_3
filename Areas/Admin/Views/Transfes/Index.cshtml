@model X.PagedList.IPagedList<Project_sem_3.Models.Transfer>
@using X.PagedList.Mvc.Core
@{
    Layout = "_Admin";
}



<div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-24">
    <h6 class="fw-semibold mb-0">Transfes List</h6>

</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}

<div class="card h-100 p-0 radius-12">
    <div class="card-header border-bottom bg-base py-16 px-24 d-flex align-items-center flex-wrap gap-3 justify-content-between">

        <form id="filterForm" method="get" class="d-flex align-items-center gap-2 navbar-search">
    
      
            <div class="position-relative">
                <input type="text"
                       name="q"
                       value="@ViewBag.q"
                       class="bg-base h-40-px w-auto ps-10 pe-10 radius-12"
                       placeholder="Search by name..." />

            </div>


        </form>
    </div>

    <div class="card-body p-24">
        <div class="table-responsive scroll-sm">
            @if (TempData["Success"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    @TempData["Success"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    @TempData["Error"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            <table class="table bordered-table sm-table mb-0">
                <thead>
                    <tr>

                        <th scope="col">Candidates</th>
                        <th scope="col">TransferDate</th>
                        <th scope="col">FromStage</th>
                        <th scope="col">ToStage</th>
                        <th scope="col" class="text-center">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Candidate.Fullname</td>
                            <td>@item.TransferDate</td>
                            <td>@item.FromStage</td>
                            <td>@item.ToStage</td>

                            

                            <td class="text-center">
                                <div class="d-flex align-items-center gap-10 justify-content-center">
                                    <a asp-controller="Transfes" asp-action="Details" asp-route-id="@item.Id"
                                       class="bg-success-focus text-success-600 bg-hover-success-200 fw-medium w-40-px h-40-px d-flex justify-content-center align-items-center rounded-circle" title="View Details">
                                        <iconify-icon icon="majesticons:eye-line"
                                                      class="icon text-xl"></iconify-icon>
                                    </a>

                                </div>
                            </td>
                        </tr>
                    }
                </tbody>

            </table>
        </div>

        <div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mt-24">
            <span>
                Showing @Model.FirstItemOnPage to @Model.LastItemOnPage of @Model.TotalItemCount entries
            </span>

            <ul class="pagination d-flex flex-wrap align-items-center gap-2 justify-content-center">
                <!-- Nút Prev -->
                <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                    <a class="page-link bg-neutral-200 text-secondary-light fw-semibold radius-8 border-0 d-flex align-items-center justify-content-center h-32-px w-32-px text-md"
                       asp-action="Index" asp-route-page="@(Model.PageNumber - 1)">
                        <iconify-icon icon="ep:d-arrow-left"></iconify-icon>
                    </a>
                </li>

                <!-- Các số trang -->
                @for (int i = 1; i <= Model.PageCount; i++)
                {
                    <li class="page-item">
                        <a class="page-link fw-semibold radius-8 border-0 d-flex align-items-center justify-content-center h-32-px w-32-px text-md @(Model.PageNumber == i ? "bg-primary-600 text-white" : "bg-neutral-200 text-secondary-light")"
                           asp-action="Index" asp-route-page="@i">@i</a>
                    </li>
                }

                <!-- Nút Next -->
                <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                    <a class="page-link bg-neutral-200 text-secondary-light fw-semibold radius-8 border-0 d-flex align-items-center justify-content-center h-32-px w-32-px text-md"
                       asp-action="Index" asp-route-page="@(Model.PageNumber + 1)">
                        <iconify-icon icon="ep:d-arrow-right"></iconify-icon>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</div>

<script>
    const form = document.getElementById("filterForm");
    const searchInput = form.querySelector("input[name='q']");
    // Khi gõ vào ô tìm kiếm => đợi 0.5s rồi tự submit
    let timeout;
    searchInput.addEventListener("input", () => {
        clearTimeout(timeout);
        timeout = setTimeout(() => form.submit(), 500);
    });
</script>